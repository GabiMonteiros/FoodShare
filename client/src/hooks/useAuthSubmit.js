// import axios from "..axios"; //fora da pasta hookes, file solto
// import { useState } from "react";

// export function useAuthSubmit(url, stateValues) {
//     const [error, setError] = useState();

//     const handleSubmit = async (e) => {
//         e.preventDefault();
//         const { data } = await axios.post(url, stateValues);
//         data.userId ? location.replace("/") : setError(true);
//     };
//     return [error, handleSubmit];
// }
